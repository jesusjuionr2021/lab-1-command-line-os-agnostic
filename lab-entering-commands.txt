Lab - Entering Commands
==========
Follow the instructions line-by-line.  

* Type in the commands as is, but ignore the beginning prompt.  
* Enter, tab, up and down are represented by <ENTER><TAB>,<UP> and <DOWN>.  
* "No output" or "nothing happens" are valid answers to any of the questions.
==========



==========
1. Open Terminal.  Type: Jesus

$ ls<ENTER>

Copy and paste the output of ls below.

jesusporrasjr

----------
Code  Desktop  Documents  Downloads  Movies  Music  Pictures  textfile.txt


==========
2a. Type: 

$ ls D<TAB>

What happens?  Describe it below.

It doesn't respond to the command. 

----------
Desktop/    Documents/  Downloads/


==========
2b. Press <TAB> one more time.  Copy and paste the output below.

.bash_history   .bash_logout    .bashrc         .cache/         .config/        .profile        .vscode-server/ .wget-hsts      jesusporrasjr/ 

----------
rdrgeerwtger yg erwy brg


==========
2c. Now press <ENTER>. Copy and paste the output below.
----------

ls: cannot access 'D': No such file or directory

==========
3.  Type:

$ ls -l Documents<ENTER>

Copy and paste the first *two lines* of output below.

ls: cannot access 'Documents': No such file or directory

----------



==========
4.  Type:

$ ls -a Documents

Copy and paste the first *two lines* of output below.

ls: cannot access 'Documents': No such file or directory

----------



==========
5.  Type:

<UP><ENTER>

What command was repeated?

$ ls -l Documents

----------



==========
6.  Type:

<UP><UP><UP><DOWN><ENTER>

What command was repeated?

ls -l Documents

----------



==========
7.  Type:

$ ls /b<TAB><ENTER>

What is the result of pressing tab?

ls: cannot access '/b': No such file or directory

----------



==========
8.  Type:

$ ls doesnotexist

Copy and paste the output below.

ls: cannot access 'doesnotexist': No such file or directory

----------



==========
9.  What key do I press to go to the previous command?

Up

----------



==========
10.  Name two flags that we used in this exercise.

N/A because of the error command and needed to review with the instructor

----------



==========
11.  What key do I use to attempt to autocomplete a command or a file name?

N/A beacuse of the error command and needed to review with the instructor

----------



==========
12.  What was the main command that we used throughout this exercise?

ls 

----------



==========
13.  Name one argument that we passed to the command.

N/A because of the error command and needed to review with the instructor
----------



==========
14.  Type:

$ foo<ENTER>

Copy and paste the output below.

Command 'foo' not found, did you mean:

  command 'fio' from deb fio
  command 'woo' from deb python-woo
  command 'fog' from deb ruby-fog
  command 'fop' from deb fop
  command 'goo' from deb goo
  command 'fgo' from deb fgo
  command 'fox' from deb objcryst-fox

Try: sudo apt install <deb name>

----------



==========
15.  Type:

<UP><ENTER>

Copy and paste the output below.

Command 'foo' not found, did you mean:

  command 'fop' from deb fop
  command 'fog' from deb ruby-fog
  command 'fio' from deb fio
  command 'goo' from deb goo
  command 'fgo' from deb fgo
  command 'fox' from deb objcryst-fox
  command 'woo' from deb python-woo

Try: sudo apt install <deb name>

----------



==========
16.  In the following line....

$ ls -a Desktop

What is the command, flag, argument, and prompt?

ls: cannot access 'Desktop': No such file or directory

----------
